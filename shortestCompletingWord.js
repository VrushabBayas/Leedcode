/**
 * @param {string} licensePlate
 * @param {string[]} words
 * @return {string}
 */
const checkSubStr = (str1, str2) => {};
var shortestCompletingWord = function (licensePlate, words) {
  let updatedString = licensePlate.replace(/[^a-zA-z]/g, "");
  let result = words.filter((word) => {});
  return updatedString;
};

const res = shortestCompletingWord("1s3 PSt", [
  "step",
  "steps",
  "stripe",
  "stepple",
]);

res;
